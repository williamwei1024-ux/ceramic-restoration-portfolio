# 陶瓷修复作品集 · 线上翻书画册 — 完整复刻提示词

> 本文档记录了从零构建到最终效果的完整实现思路、全部踩过的坑与修复方法，以及最终状态的详细规格。
> 使用方式：把「一、核心提示词」整段发给 AI，即可从零复刻；其余部分是规格与踩坑记录，供调整时参考。

---

## 一、核心提示词（可直接复刻）

```
请帮我构建一个「陶瓷修复作品集」线上翻书画册网站（纯静态 HTML/CSS/JS），按以下规格实现：

【内容来源】
- 一本 24 页的 PDF（每页是修复前/后对比图 + 年代/尺寸标注），用 PyMuPDF 把每页渲染成 1400 宽 JPEG 存到 pages/p01.jpg~p24.jpg
- 内容结构：封面 → 扉页（引言页）→ 目录页 → 24 页内页 → 封底

【翻书效果】
- 使用 StPageFlip（page-flip 库 2.x，本地化到 js/vendor/），真实翻书动画
- 桌面宽屏（≥641px）：双页对开模式；移动端窄屏：单页模式（usePortrait:true + minWidth:340 触发自动切换）
- 封面/封底闭合时书居中；翻开后平摊对开
- 翻页交互：拖拽/滑动、底部 Prev/Next 按钮、键盘 ←/→/空格、目录跳转

【封面/封底】
- 封面：深蓝底 + 巨型「修」字（白色填充 + 2px 冷蓝描边）+ 字内蓝色线稿花纹（background-clip:text 蒙版）
- 封底：深蓝底 + 巨型「復」字（同上，用修复后线稿）
- 字内花纹：稀疏线稿（只保留主要线条，opacity 0.45，白底为主）+ 封面同款蓝 RGB(125,157,189)
- 封面/封底底部：中文副题 + 英文作者名（无衬线细体）

【扉页/目录页】
- 扉页：英文小标 + 引言「每一件破损的器物，都是一段等待续写的历史。」+ 分隔线（正文色）
- 目录页：作品清单 + 页码，点线引导对齐，页码右对齐
- 两页均浅蓝底 #c6ddf3

【网页配色主题】
- 网页底色 = 每件作品「修复方式栏」的色块颜色（从 PDF 提取），实色不透明度 100%
- 同一作品内所有页保持同一色（作品级统一）
- 深色背景时（亮度<140）自动切换浅色文字/按钮（.dark-bg 模式）
- 目录侧栏背景比底色稍浅 12%；导航栏比底色深 12%
- 导航栏/目录侧栏/翻页按钮的英文和数字颜色与汉字一致（深浅模式都统一）

【导航栏】
- 顶部工具栏：目录 / 书名 / 另一个我（跳转 https://wlyphoto.netlify.app/）
- 底部翻页区：Prev · 页码 · Next · 首尾按钮（封面/封底间切换）
- 无放大按钮（点击书页内容放大查看）
- 网站标题：LIYUAN WEI | Restoration

【图片处理】
- 每页主题色：从 PDF 修复方式栏色块精确提取（get_drawings 找矩形填充，优先彩色，白黑也保留）
- 字内线稿：从 PDF 第13页（鹦鹉纹枕）修复前/后图提取，染成封面蓝 RGB(125,157,189)，alpha 全满
- 高清线稿备份存到「高清线稿/」目录

【已知坑（务必避免）】
- 库的 changeState 回调签名是单个对象 {data, object}，不是 (e,data)
- 移动端单页模式 flipPrev() 失灵，需改用 turnToPage(idx-1)
- 封面闭合平移：isDouble 是函数要加括号调用，否则移动端也平移导致书偏左
- 翻页跳动：封面 transform 平移要加 transition，翻页动画中不改 transform，翻转后复位
- turnToPage 到末尾可能不触发 changeState read，需 setTimeout 兜底复位 isFlipping
- 字内蒙版 background-clip:text 需给 mask 元素加文字内容，且 -webkit-text-fill-color:transparent 防多字
- 内部边线若用 border 会导致左右页图片跨页错位，改用 ::after 绝对定位（或取消中线）
- 移动端翻页按钮组要 flex-wrap 防横向溢出

【部署】
- git 推送到 GitHub，Netlify 连接仓库自动部署（纯静态无需构建）
```

---

## 二、最终规格（当前状态）

### 页面结构（28 页）
```
封面「修」→ 扉页(引言) → 目录页 → 内页p01~p24 → 封底「復」
```

### 关键参数
| 项目 | 值 |
|------|-----|
| 书名 | 陶瓷修复作品集 |
| 网站标题 | LIYUAN WEI \| Restoration |
| 书尺寸 | 960×680（BOOK_W/H），autoSize 自适应 |
| 单页/对开切换 | ≥641px 双页，否则单页 |
| 作品数 | 9 件（p1~p24，含修复前/后对开） |
| 封面字 | 白填充 + 2px 描边 `--cyan-ink`(#bcd4e8)，字内蓝花纹 RGB(125,157,189) |
| 网页底色 | 作品修复栏色块色（WORK_THEME 数组，10 色） |
| 附加页底 | #c6ddf3 浅蓝 |
| 深色阈值 | 亮度 < 140 触发 dark-bg |

### 主题色表（WORK_THEME）
```
作品1 隋高足盘:      rgb(206,196,185)
作品2 矾红彩盘:      rgb(170,111,93)
作品3 吉州窑黑釉盏:  rgb(247,240,218)
作品4 白覆轮黑釉盏:  rgb(79,72,69)
作品5 巩县窑白釉盏:  rgb(200,200,200)
作品6 鹦鹉纹枕:      rgb(96,137,109)
作品7 连枝树形灯:    rgb(168,168,144)
作品8 庖厨俑:        rgb(229,218,206)
作品9 铅釉陶楼:      rgb(144,144,144)
（收尾页并归作品9，无独立条目）
```

### 目录侧栏
- 封面·修 → 9 作品（页码）→ 封底·復
- 背景比底色浅 12%、文字深浅适配

---

## 三、bug 修复清单（本会话共 15 次提交）

| # | 问题 | 修复 |
|---|------|------|
| 1 | 初版中式风格太土 | 改成极简杂志风（白底/无圆角/Playfair） |
| 2 | 封面/封底不像书 | 用 StPageFlip 做真实翻书 + 封面/封底 |
| 3 | 页面间有空隙 | 双页贴合、跨页去边框 |
| 4 | 封面两页 | 去书壳、封面单页居中（transform 平移） |
| 5 | 移动端横向溢出 | 翻页按钮 flex-wrap |
| 6 | 移动端书比例错乱 | usePortrait:true + minWidth 转单页模式 |
| 7 | 移动端封面偏左 | isDouble 加括号调用 |
| 8 | 翻页跳动 | 封面 transform 加 transition + flip 时不改 transform |
| 9 | 首尾跳转不居中 | flip 超时兜底复位 isFlipping |
| 10 | 移动端前翻失灵 | 单页模式 flipPrev 改 turnToPage |
| 11 | 主题色提取偏 | 用修复方式栏色块（get_drawings 取矩形填充） |
| 12 | 字内花纹消失/全蓝 | 线稿 alpha 全满 + 稀疏化 + opacity 0.45 |
| 13 | 封面封底多字 | giant-char z-index 分层 + mask 文字 transparent |
| 14 | 第22页图片错位 | 内部边线改 ::after 绝对定位（后取消中线） |
| 15 | 其他迭代 | 标题/作者名/按钮/文案/跳转地址等细节 |

---

## 四、文件结构

```
修复陶瓷画册/
├── index.html                    # 工具栏 + 书 + 目录 + lightbox
├── css/style.css                 # 全部样式（含深浅模式适配）
├── js/main.js                    # CONFIG / WORK_THEME / 翻页逻辑
├── js/vendor/page-flip.browser.js  # StPageFlip 库（本地化）
├── assets/                       # 封面/封底 线稿（light/deep 两版）
├── pages/p01~p24.jpg             # PDF 渲染的页面图
├── 高清线稿/                     # 提取的原始高清线稿备份
└── README.md
```
